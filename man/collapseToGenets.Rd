% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapseToGenets.R
\name{collapseToGenets}
\alias{collapseToGenets}
\title{Collapse colonies to one representative per genet}
\usage{
collapseToGenets(
  dataset,
  genetAssignment,
  representative = c("most_data", "first"),
  drop_unassigned = FALSE
)
}
\arguments{
\item{dataset}{A data frame with a \code{Coral_ID} column (plus any genotype
columns). Rows are selected from this frame.}

\item{genetAssignment}{A genet-assignment data frame with at least \code{Coral_ID}
and \code{genet} columns (and, ideally, \code{pctNull}), as returned per file by
\code{\link[=runGenets]{runGenets()}}.}

\item{representative}{How to choose the representative colony within a genet:
\code{"most_data"} (default) keeps the colony with the most scored loci (lowest
\code{pctNull}); \code{"first"} keeps the first colony listed. \code{"most_data"} falls
back to \code{"first"} if no \code{pctNull} column is present.}

\item{drop_unassigned}{Logical. "Unassigned" colonies are those with no real
genet -- i.e. colonies with inadequate data (their self-comparison fell
below the \code{PctNotNull} threshold) or all-\code{NA} colonies, carrying a missing
or \code{NA}-suffixed genet label. Because they were never placed in a genet,
they are not clonal duplicates of one another. The default (\code{FALSE}) retains
each as its own unit, so no colonies are silently discarded; this suits
eligible-pool questions (every colony is a real candidate). Set \code{TRUE} to
drop them for a cleaner genet-level population estimate, where these
low-data, unplaceable colonies would otherwise add noise. Note that \code{TRUE}
removes rows, so the result will have fewer colonies than the input.}
}
\value{
A subset of \code{dataset} containing one row per genet plus (unless
dropped) each unassigned colony. Column structure and order are unchanged.
}
\description{
Reduces a genotype data frame to a single representative colony
per genet, using a set of genet assignments from \code{\link[=runGenets]{runGenets()}}. This is a
pre-filter for clone-corrected analyses: feeding the result to the kinship
pipeline yields genet-level (clone-corrected) gene diversity rather than the
eligible-pool diversity computed over all colonies (see \code{\link[=runKinship]{runKinship()}}).

The function only filters rows; it does not alter genotype values, so it can
be applied to either raw paired-allele data (e.g., from \code{\link[=readGeneticData]{readGeneticData()}})
or single-letter coded data (from \code{\link[=convertBasePairstoCodes]{convertBasePairstoCodes()}}). It matches
rows on \code{Coral_ID}, so identifiers may contain any characters.
}
\examples{
\dontrun{
# Clone-corrected (genet-level) kinship and gene diversity:
g   <- runGenets(PctMatchThreshold = 90, PctNotNullThreshold = 50)
ga  <- g$genetAssignments[[1]]
raw <- readGeneticData(file.path(getwd(), "Data", "myfile.csv"))[[1]]

reduced <- collapseToGenets(raw, ga)                 # one colony per genet

coded   <- convertBasePairstoCodes(reduced)
withDat <- isolateAllNAColonies(coded)[[1]]
noInv   <- omitInvariantLoci(withDat)
k       <- kinshipCalcsNoInvar(noInv, subset = FALSE)
k$PopAvgMKGD                                          # clone-corrected diversity
}
}
\seealso{
\code{\link[=runGenets]{runGenets()}} to produce \code{genetAssignment}; \code{\link[=runKinship]{runKinship()}} and
\code{\link[=kinshipCalcsNoInvar]{kinshipCalcsNoInvar()}} for the kinship pipeline this feeds.
}
