% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeKinship.R
\name{computeKinship}
\alias{computeKinship}
\title{Compute kinship and gene diversity from a genotype data frame}
\usage{
computeKinship(data, subset = FALSE, targetN = NULL)
}
\arguments{
\item{data}{A genotype data frame with a \code{Coral_ID} column and paired-allele
SNP columns (e.g., \code{"A:G"}), as produced by \code{\link[=readGeneticData]{readGeneticData()}} or
\code{\link[=collapseToGenets]{collapseToGenets()}}. A \code{MatchMaker_Index} column, if present, is ignored.
Missing values may be \code{NA} or \code{"?"}.}

\item{subset}{Logical; if \code{TRUE}, also compute the \code{targetN}-colony summary.
Requires \code{targetN}. Default \code{FALSE}.}

\item{targetN}{Number of least-related colonies to retain when
\code{subset = TRUE}. Must be >= 2.}
}
\value{
A list with \code{PopAvgMKGD}, \code{MK_init}, and \code{MK_final} (see
\code{\link[=kinshipCalcsNoInvar]{kinshipCalcsNoInvar()}}).
}
\description{
Data-frame entry point to the kinship pipeline: takes a genotype
data frame directly (no \code{Data}/\code{Results} folders) and returns
individual- and population-level mean kinship and gene diversity. This is
the in-memory counterpart to \code{\link[=runKinship]{runKinship()}}, useful when the genotypes are
already in R -- for example after collapsing to one colony per genet with
\code{\link[=collapseToGenets]{collapseToGenets()}} for a clone-corrected estimate.

Internally it runs the same steps as \code{\link[=runKinship]{runKinship()}}: translate paired
alleles to IUPAC codes (\code{\link[=convertBasePairstoCodes]{convertBasePairstoCodes()}}), set aside all-\code{NA}
colonies (\code{\link[=isolateAllNAColonies]{isolateAllNAColonies()}}), optionally drop invariant loci, and
compute kinship (\code{\link[=kinshipCalcsNoInvar]{kinshipCalcsNoInvar()}}). Results are keyed by \code{Coral_ID}.
}
\examples{
\dontrun{
raw <- readGeneticData(file.path(getwd(), "Data", "myfile.csv"))[[1]]

# eligible-pool kinship (all colonies, ramets included):
computeKinship(raw)

# clone-corrected kinship (one colony per genet):
ga <- runGenets(PctMatchThreshold = 90, PctNotNullThreshold = 50)$genetAssignments[[1]]
computeKinship(collapseToGenets(raw, ga))
}
}
\seealso{
\code{\link[=runKinship]{runKinship()}} for the folder-based workflow; \code{\link[=collapseToGenets]{collapseToGenets()}}
to reduce to one colony per genet before computing a clone-corrected result.
}
