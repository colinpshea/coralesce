% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flagPossibleReplicates.R
\name{flagPossibleReplicates}
\alias{flagPossibleReplicates}
\title{Flag possible replicate (re-run) samples}
\usage{
flagPossibleReplicates(dataset)
}
\arguments{
\item{dataset}{A data frame with a \code{Coral_ID} column.}
}
\value{
Invisibly, a data frame of the flagged pairs (\code{replicate} and the
\code{original} it appears to repeat), or \code{NULL} if none are found. Called mainly
for its message side effect.
}
\description{
Detects \code{Coral_ID}s that look like repeat observations of the
same physical colony, and emits an informative message so they can be
reviewed before analysis. Replicate/re-run samples are typically named by
appending a suffix to the original's ID (e.g., \code{ACER_001} and
\code{ACER_001_Rerun}, or \code{ACER_001} and \code{ACER_001_1}). This function flags any
\code{Coral_ID} that contains another \code{Coral_ID} as a punctuation-bounded prefix,
which catches those cases regardless of the exact suffix wording (\verb{_Rerun},
\verb{_1}, \verb{_rep}, etc.).

This is a \strong{non-blocking} heads-up, not an error. Nothing is wrong with the
computation: genet assignment handles any identifier correctly. The reason
to review is that leaving replicates in the data inflates the genet count
and biases diversity/kinship, because a replicate is a second observation of
a colony that is already represented. Decide whether to remove replicates
(or keep the higher-coverage record per colony) before interpreting results.
}
