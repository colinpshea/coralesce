% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeGenets.R
\name{computeGenets}
\alias{computeGenets}
\title{Assign colonies to genets from a genotype data frame}
\usage{
computeGenets(
  data,
  PctMatchThreshold = NULL,
  PctNotNullThreshold = NULL,
  speciesCode = NULL,
  getPairwiseAlleleMatches = FALSE
)
}
\arguments{
\item{data}{A genotype data frame with a \code{Coral_ID} column and paired-allele
SNP columns (e.g., \code{"A:G"}), as produced by \code{\link[=readGeneticData]{readGeneticData()}}. A
\code{MatchMaker_Index} column, if present, is carried through and used to order
the output. Missing values may be \code{NA} or \code{"?"}.}

\item{PctMatchThreshold}{Minimum percent allele match across loci for two
colonies to be called a clone. Required.}

\item{PctNotNullThreshold}{Minimum percent of loci with valid data required to
trust a comparison. Required.}

\item{speciesCode}{Optional short code prefixed to each genet label (e.g.,
\code{"ACER"} gives \code{ACER_00001}). If \code{NULL} (default) genet labels are the
zero-padded number alone (\code{00001}). Colonies without a genet (inadequate or
all-\code{NA} data) carry a label ending in \code{NA}.}

\item{getPairwiseAlleleMatches}{Logical; if \code{TRUE}, also return the full
pairwise comparison table. Default \code{FALSE}.}
}
\value{
If \code{getPairwiseAlleleMatches = FALSE}, a genet-assignment data frame
with columns \code{Coral_ID}, (\code{MatchMaker_Index} if supplied,) \code{genet},
\code{pctNull}, \code{AdequateData}. If \code{TRUE}, a list of that data frame plus the
pairwise table.
}
\description{
Data-frame entry point to the genet-assignment pipeline: takes a
genotype data frame directly (no \code{Data}/\code{Results} folders) and returns genet
assignments. This is the in-memory counterpart to \code{\link[=runGenets]{runGenets()}}, useful when
the genotypes are already in R -- in particular, its output is the
\code{genetAssignment} that \code{\link[=collapseToGenets]{collapseToGenets()}} needs, so the whole
clone-corrected workflow can run without files.

Internally it runs the same steps as \code{\link[=runGenets]{runGenets()}}: translate paired alleles
to IUPAC codes, set aside all-\code{NA} colonies, compare all colony pairs, and
assign genets (\code{\link[=groupByGenets]{groupByGenets()}}). Results are keyed by \code{Coral_ID}.
}
\examples{
\dontrun{
raw <- readGeneticData(file.path(getwd(), "Data", "myfile.csv"))[[1]]
ga  <- computeGenets(raw, PctMatchThreshold = 90, PctNotNullThreshold = 50)

# fully folder-free clone-corrected diversity:
computeKinship(collapseToGenets(raw, ga))
}
}
\seealso{
\code{\link[=runGenets]{runGenets()}} for the folder-based workflow; \code{\link[=collapseToGenets]{collapseToGenets()}} to
reduce to one colony per genet; \code{\link[=computeKinship]{computeKinship()}} for the kinship pipeline.
}
